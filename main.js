var isbookavailable = true;
var userborrowedbooks = 2;
var maxAllowedbooks = 3;
if (isbookavailable && userborrowedbooks < maxAllowedbooks) {
    console.log("ap book borrow krsaktay hain!");
}
else {
    console.log("ap book borrow n krsaktay ");
}
